package API.BigDecimalAPI;

import java.math.BigDecimal;

public class BigDecimalAPI1 {
    static void main(String[] args) {
        //写等号右边然后ctrl＋alt＋v可以自动生成左边
        BigDecimal hehe = new BigDecimal(0.1);
    }
}
